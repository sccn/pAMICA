*.py[co]
*~
*.DS_Store
_build/
auto_examples/
gen_api/

# Ignore everything in the egg-info directory
pamica.egg-info/*

# Generated run/validation output
output/
validation_output/
enhanced_test_output/

# AGENTS.md, CLAUDE.md, .rules/, and .context/ are tracked in git.
# Working docs (plan/research/ideas/scratch) now live in .context/.

# Claude epic state (local only)
.claude/*.local.md

# Coverage artifacts
.coverage
coverage.xml
htmlcov/

# Temp output from NG e2e test (Fortran run artifacts)
pamica/tests/torch_tests/_ng_e2e_tmp/
pamica/tests/torch_tests/_ng_e2e_json_tmp/

# Benchmark run artifacts
benchmarks/results/
# Real EEG benchmark data (fetched from OpenNeuro, not committed) + result JSONs
benchmarks/data/
benchmarks/*.json
# Native amica build artifacts (#85; hardware-specific, built per host via
# benchmarks/fortran/build_amica.sh -- Apple Silicon or the x86 host, not in CI)
benchmarks/fortran/amica15
benchmarks/fortran/build/
bench_out/

# Local secrets (never commit)
.env
.env.*

# Decomposition benchmark run artifacts (#87)
results/

# MkDocs build output
site/

# Regenerated multi-model ensemble data (issue #27); recreate with multimodel_ensemble.py
.context/issue-27/ensemble.npz

# Regenerated MATLAB-gate artifacts (issue #159); recreate with gate_prep.py + gate_compare.m
.context/issue-159/gate_2model/
.context/issue-159/py_*.npz
.context/issue-159/mat_*.mat

# Claude Code local state (settings, agent worktrees) -- machine-local, never tracked
.claude/
